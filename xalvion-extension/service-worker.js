// Popup-based version (stable, no sidePanel bugs)

console.log("Xalvion Assist background loaded");

// Optional: keep for future messaging/logging
chrome.runtime.onInstalled.addListener(() => {
  console.log("Xalvion installed");
});